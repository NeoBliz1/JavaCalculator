import java.util.regex.Pattern;
import java.util.regex.Matcher;

public class Main {
    static String calc(String equation) {

        return equation;
    }

    public static void main(String[] args) {
        System.out.println(calc(args[0]));
    }
}